type Proposal @entity {
  id: ID!
  votesCounter: BigInt!
  weigthCounter: BigInt!
  totalYesVotes: BigInt!
  totalNoVotes: BigInt!
  totalYesWeight: BigInt!
  totalNoWeight: BigInt!
}

type GlobalVote @entity {
  id: ID! 
  votesCounter: BigInt!
  weigthCounter: BigInt!
}